import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'bfs-support',
  changeDetection: ChangeDetectionStrategy.OnPush,
  templateUrl: './support.html',
})
export class Support {}
