// The day Manu supplied the text. It moves only when the wording itself changes — never on a
// merge date, and never because a processor record was updated.
export const PRIVACY_LAST_UPDATED = '2026-08-17';
